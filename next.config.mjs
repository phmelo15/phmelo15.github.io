/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // Adicione o caminho base do seu projeto GitHub
  basePath: "/phmelo15.github.io",
  trailingSlash: true,
};

export default nextConfig;
